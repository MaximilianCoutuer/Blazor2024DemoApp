@page "/exercise-page-results/{X:int}/{Y:int}"
<h3>ExercisePageResultsPage</h3>
<p>X = @X, Y = @Y, Area = @(X * Y)</p>

@code {
    [Parameter]
    public int X { get; set; }
    [Parameter]
    public int Y { get; set; }
}
